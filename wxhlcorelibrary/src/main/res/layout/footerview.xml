<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_marginBottom="4dp" android:visibility="gone">

    <ProgressBar android:layout_toLeftOf="@+id/footerTitle"
        android:id="@+id/pro" android:layout_width="wrap_content"
        android:layout_height="24dp" />
    <TextView android:id="@+id/footerTitle" android:layout_width="wrap_content"
        android:layout_height="24dp" android:text="加载中。。"
        android:layout_centerHorizontal="true" android:textColor="@color/black"
        android:gravity="center_vertical"/>
</RelativeLayout>